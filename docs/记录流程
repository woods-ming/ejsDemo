admin: 
npm install -g gulp

project:
npm init
npm install del --save-dev（删除文件）
npm install glob --save-dev（目录匹配）
npm install wiredep --save-dev（依赖注入）
npm install gulp --save-dev（执行任务）
npm install gulp-notify --save-dev（发出通知）
npm install gulp-ejs-json --save-dev（使用ejs引擎）
npm install gulp-minify-html --save-dev（压缩html）
npm install gulp-server-livereload --save-dev（本地服务器）


bower init
bower install bootstrap --save
bower install google-code-prettify --save

创建gulpfile.js
创建views目录，编辑*.ejs
创建models目录，编辑*.json